var HomeConfig = function (app, express) {
    
    'use strict';
    
    this.displayName = 'HomeConfig';
    
    var setup;
    
    setup = function (app, express) {
        
        // for future use
    };
    
    setup(app, express);

};

module.exports = HomeConfig;